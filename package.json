{
  "name": "srt-player",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "parcel index.html",
    "clean": "rm -rf dist/ .cache/",
    "prepublish": "rm -rf dist/",
    "publish": "parcel build --no-source-maps --public-url . index.html && surge dist srtplayer.surge.sh"
  },
  "dependencies": {
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "surge": "*"
  }
}
